import React from 'react';

const LoadProblem = ({problem}) => {
    return (
        <div>
            <div>
                <h1>{problem.title}</h1>
            </div>
            
        </div>
    );
};

export default LoadProblem;